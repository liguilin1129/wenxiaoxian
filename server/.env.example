# 请只在服务端填写，绝不能提交真实值或放入 miniprogram 目录。
PORT=3000
WECHAT_APP_ID=wx40a0b7c7aeb9d18a
WECHAT_APP_SECRET=replace-with-your-app-secret
# 使用至少 32 位随机字符串，例如：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_TOKEN_SECRET=replace-with-a-long-random-secret
